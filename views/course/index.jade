extends ../layout

block content
	.content
		.courses_block
			.title
				h2 Choose a topic
			.scroll
				ol
					- for (var item in course)
						li
							a.list(href='/courses/' + course[item].cathegory + '/' + course[item]._id) #{course[item].title}