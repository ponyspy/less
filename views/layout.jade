html
	head
		include header
	body
		.main
			.bg
				a(href='/')
					.head
						.head_text
							.head_text_title
								|Spring 2013, it’s time to study online!
							br
							|Choose a topic for your consultation, choose time 
							br
							|and teachers from the Center for international Education of Lomonosov Moscow State University will answer all your questions.
				//- .flags
				//- 	a(href='/rus')
				//- 		img.flag(src='/img/design/rus.png')
				//- 	|&nbsp;&nbsp;&nbsp;&nbsp;
				//- 	a(href='/eng')
				//- 		img.flag(src='/img/design/eng.gif')
				- if (session.user_id)
					.auth= session.name
						- if (session.status == 'Admin')
							|&nbsp;|&nbsp;
							a(href='/auth') Звездочет
						|&nbsp;|&nbsp;
						a(href='/logout') Logout
				- else
					.auth
						a(href='/') Sign in 
						|&nbsp;|&nbsp;
						a(href='/registr') Sign Up
				block content
				.menu
					- if (menu == 'courses')
						.item.select
							a.item_text(href='/courses') COURSES
					- else
						.item.courses
							a.item_text(href='/courses') COURSES
					- if (session.user_id)
						- if (menu == 'you')
							.item.select
								a.item_text(href='/you') ВАШИ КУРСЫ
						- else
							.item.you
								a.item_text(href='/you') ВАШИ КУРСЫ
					- if (menu == 'contacts')
						.item.select
							a.item_text(href='/contacts') CONTACT US
					- else
						.item.contacts
							a.item_text(href='/contacts') CONTACT US
					- if (menu == 'about')
						.item.select
							a.item_text(href='/about') ABOUT
					- else
						.item.about
							a.item_text(href='/about') ABOUT
				img.cloud(src='/img/design/cloud.png')
				a(href='http://www.speak-russian.cie.ru', target='_blank')
					.cie
						.cie_text
							| Based on the course
							br
							b “Time to speak Russian!” 